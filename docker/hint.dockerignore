# GOOD .dockerignore pattern — add next to your Dockerfile and copy as `.dockerignore` at build context root.
# A real project should also ignore: .env*, secrets/, .git, coverage/, test snapshots you do not need in the image

node_modules
dist
.next
.turbo
.git
.gitignore
**/*.log
**/*.test.ts
.env
.env.*
