# HINT: combine app + dependencies for local dev — not production as-is
# docker compose -f docker/docker-compose.hint.yml up --build
# Replace `image: fundamentals-demo:good` with a service built from good.Dockerfile
services:
  app:
    build:
      context: ..
      dockerfile: docker/good.Dockerfile
    ports:
      - "3000:3000"
    restart: unless-stopped
    # env_file: .env   # do not commit secrets; use in dev only
  # Add postgres/redis when you wire real demos
